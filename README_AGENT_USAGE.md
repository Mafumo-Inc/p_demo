# 🤖 エージェント使用方法ガイド

## ⚠️ 重要な注意事項

### メインプロジェクトディレクトリから実行（推奨）

**エージェントコマンドは必ずメインプロジェクトディレクトリから実行してください。**

```bash
# ✅ 正しい実行方法（推奨）
cd /Users/masafumikikuchi/Dev/p_demo
npm run agent:run
npm run agent:progress
npm run agent:watch

# ❌ 間違った実行方法（worktreeディレクトリから実行）
cd /Users/masafumikikuchi/Dev/p_demo-worktrees/agent-frontend-20251112
npm run agent:run  # エラー: agent:progressスクリプトが見つからない
```

### worktreeディレクトリから実行する場合

worktreeディレクトリから実行する場合は、**直接スクリプトを実行**してください。

```bash
# worktreeディレクトリから実行
cd /Users/masafumikikuchi/Dev/p_demo-worktrees/agent-frontend-20251112

# 直接スクリプトを実行（メインプロジェクトディレクトリを自動検出）
bash ../p_demo/scripts/run-agents.sh backend
bash ../p_demo/scripts/check-agent-progress.sh
bash ../p_demo/scripts/watch-agent-progress.sh
```

**注意**: worktreeディレクトリから`npm run`コマンドを実行すると、worktreeディレクトリの`package.json`が参照されるため、メインプロジェクトのスクリプトは実行できません。

## 📋 エージェントコマンド一覧

### 基本コマンド

```bash
# メインプロジェクトディレクトリに移動
cd /Users/masafumikikuchi/Dev/p_demo

# 認証セットアップ
npm run agent:auth

# エージェント環境セットアップ
npm run agent:setup

# 全エージェントを並列実行
npm run agent:run

# 個別エージェントを実行
npm run agent:run:architect
npm run agent:run:backend
npm run agent:run:frontend
npm run agent:run:testing
npm run agent:run:review

# 進捗確認（1回）
npm run agent:progress

# 進捗監視（リアルタイム）
npm run agent:watch

# 状態確認
npm run agent:status
```

## 🔍 トラブルシューティング

### エラー: `Missing script: "agent:progress"`

**原因:**
- worktreeディレクトリから実行している
- worktreeディレクトリには`package.json`が存在しない

**解決方法:**
```bash
# メインプロジェクトディレクトリに移動
cd /Users/masafumikikuchi/Dev/p_demo

# コマンドを実行
npm run agent:progress
```

### エラー: `エージェント設定ファイルが見つかりません`

**原因:**
- worktreeディレクトリから実行している
- エージェント設定ファイルはメインプロジェクトディレクトリにある

**解決方法:**
```bash
# メインプロジェクトディレクトリに移動
cd /Users/masafumikikuchi/Dev/p_demo

# エージェント環境をセットアップ（まだの場合）
npm run agent:setup

# コマンドを実行
npm run agent:run
```

### エージェントが実行されない

**原因:**
- 認証が設定されていない
- プロンプトファイルが見つからない

**解決方法:**
```bash
# 認証セットアップ
npm run agent:auth

# エージェント環境をセットアップ
npm run agent:setup

# エージェントを実行
npm run agent:run
```

## 📝 推奨ワークフロー

### Step 1: セットアップ

```bash
# メインプロジェクトディレクトリに移動
cd /Users/masafumikikuchi/Dev/p_demo

# 認証セットアップ
npm run agent:auth

# エージェント環境セットアップ
npm run agent:setup
```

### Step 2: エージェント実行

```bash
# メインプロジェクトディレクトリから実行
cd /Users/masafumikikuchi/Dev/p_demo

# 全エージェントを並列実行
npm run agent:run
```

### Step 3: 進捗監視

```bash
# 別ターミナルで進捗を監視
cd /Users/masafumikikuchi/Dev/p_demo
npm run agent:watch
```

### Step 4: 結果確認

```bash
# メインプロジェクトディレクトリから実行
cd /Users/masafumikikuchi/Dev/p_demo

# 進捗確認
npm run agent:progress

# 各worktreeの変更を確認
cd ../p_demo-worktrees/agent-frontend-20251112
git status
git diff
```

## 💡 ヒント

1. **メインプロジェクトディレクトリから実行**: 全てのエージェントコマンドはメインプロジェクトディレクトリから実行してください

2. **別ターミナルで監視**: `npm run agent:run`を実行したら、別ターミナルで`npm run agent:watch`を実行して進捗を監視してください

3. **worktreeディレクトリは確認用**: worktreeディレクトリは結果を確認するために使用し、コマンド実行には使用しないでください

4. **認証状態を確認**: エージェントが実行されない場合、認証状態を確認してください（`npm run agent:auth`）

---

**重要**: 全てのエージェントコマンドはメインプロジェクトディレクトリ（`/Users/masafumikikuchi/Dev/p_demo`）から実行してください！

