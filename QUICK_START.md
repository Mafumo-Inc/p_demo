# 🚀 クイックスタートガイド

## 📋 エージェントコマンドの実行方法

### ⚠️ 重要な注意事項

**エージェントコマンドは必ずメインプロジェクトディレクトリから実行してください。**

```bash
# ✅ 正しい実行方法
cd /Users/masafumikikuchi/Dev/p_demo
npm run agent:progress
npm run agent:watch
npm run agent:run
```

### ❌ 間違った実行方法

```bash
# worktreeディレクトリから実行（エラーになる）
cd /Users/masafumikikuchi/Dev/p_demo-worktrees/agent-frontend-20251112
npm run agent:progress  # エラー: Missing script: "agent:progress"
```

**理由**: `npm run`コマンドは現在のディレクトリの`package.json`を参照するため、worktreeディレクトリから実行すると、worktreeディレクトリの`package.json`が参照されます。worktreeディレクトリの`package.json`には`agent:progress`スクリプトが定義されていないため、エラーになります。

## 🔧 解決方法

### 方法1: メインプロジェクトディレクトリから実行（推奨）

```bash
# メインプロジェクトディレクトリに移動
cd /Users/masafumikikuchi/Dev/p_demo

# コマンドを実行
npm run agent:progress
npm run agent:watch
npm run agent:run
```

### 方法2: worktreeディレクトリから直接スクリプトを実行

```bash
# worktreeディレクトリから実行
cd /Users/masafumikikuchi/Dev/p_demo-worktrees/agent-frontend-20251112

# 直接スクリプトを実行（メインプロジェクトディレクトリを自動検出）
bash ../../p_demo/scripts/check-agent-progress.sh
bash ../../p_demo/scripts/watch-agent-progress.sh
bash ../../p_demo/scripts/run-agents.sh backend
```

**注意**: worktreeディレクトリから実行する場合は、**メインプロジェクトディレクトリに移動してから実行することを推奨**します。

## 📝 エージェントコマンド一覧

### 基本コマンド

```bash
# メインプロジェクトディレクトリから実行
cd /Users/masafumikikuchi/Dev/p_demo

# 認証セットアップ
npm run agent:auth

# エージェント環境セットアップ
npm run agent:setup

# 全エージェントを並列実行
npm run agent:run

# 個別エージェントを実行
npm run agent:run:architect
npm run agent:run:backend
npm run agent:run:frontend
npm run agent:run:testing
npm run agent:run:review

# 進捗確認（1回）
npm run agent:progress

# 進捗監視（リアルタイム）
npm run agent:watch

# 状態確認
npm run agent:status
```

## 🔍 トラブルシューティング

### エラー: `Missing script: "agent:progress"`

**原因**: worktreeディレクトリから実行している

**解決方法**:
```bash
# メインプロジェクトディレクトリに移動
cd /Users/masafumikikuchi/Dev/p_demo

# コマンドを実行
npm run agent:progress
```

### エラー: `エージェント設定ファイルが見つかりません`

**原因**: worktreeディレクトリから実行している

**解決方法**:
```bash
# メインプロジェクトディレクトリに移動
cd /Users/masafumikikuchi/Dev/p_demo

# コマンドを実行
npm run agent:run
```

## 💡 ヒント

1. **メインプロジェクトディレクトリから実行**: 全てのエージェントコマンドはメインプロジェクトディレクトリから実行してください

2. **別ターミナルで監視**: `npm run agent:run`を実行したら、別ターミナルで`npm run agent:watch`を実行して進捗を監視してください

3. **worktreeディレクトリは確認用**: worktreeディレクトリは結果を確認するために使用し、コマンド実行には使用しないでください

---

**重要**: 全てのエージェントコマンドはメインプロジェクトディレクトリ（`/Users/masafumikikuchi/Dev/p_demo`）から実行してください！

